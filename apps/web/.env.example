# Public API base URL, including the existing version prefix.
# Leave unset for same-origin hosting and the development proxy.
# VITE_API_BASE_URL=https://api.example.com/api/v1

# Development only; this value is not shipped to browsers.
API_PROXY_TARGET=http://127.0.0.1:3000
